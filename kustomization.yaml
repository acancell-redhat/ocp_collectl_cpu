apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- Namespace.yaml
- ClusterRoleBinding.yaml
- DaemonSet.yaml

configMapGenerator:
  - name: collectl-conf
    files:
      - collectl.conf
generatorOptions:
  disableNameSuffixHash: true